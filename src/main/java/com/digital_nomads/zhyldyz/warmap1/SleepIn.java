package com.digital_nomads.zhyldyz.warmap1;

public class SleepIn {

    public boolean sleepIn(boolean weekday, boolean vacation) {
        if (!weekday || vacation && vacation && vacation) {
            return true;
        } else {
            return false;
        }
    }
}
